<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lupricht.development.de.pongaping">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".Game.Tools.Tool"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="false"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Game.MainActivity"
            android:hardwareAccelerated="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GUI.Options.Options" />
        <activity android:name=".GUI.Game.Game_Init" />
        <activity android:name=".GUI.Options.Options_Ball" />
        <activity android:name=".GUI.Options.Game_Gamemode" />
        <activity android:name=".GUI.Options.About" />
        <activity android:name=".GUI.Options.Options_wallpaper" />
        <activity android:name=".GUI.Options.Options_difficult" />
        <activity android:name=".GUI.Options.Options_music" />
        <activity android:name=".GUI.Options.Options_reset" />
        <activity android:name=".GUI.Options.Internet" />
        <activity android:name=".Net.Connection"></activity>
    </application>

</manifest>